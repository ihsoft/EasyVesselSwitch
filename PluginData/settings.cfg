UI
{
	// Allowed values: AnyControl, AnyAlt, AnyShift. You can use combinations, e.g.
	// "AnyAlt, AnyControl" in which case both modifier keys should be pressed to trigger
	// the mode.
	switchModifier = AnyAlt
	// Allowed values: Left, Right, Middle, Btn4, Btn5.
	switchMouseButton = Left
	// RGBA format.
	targetVesselHighlightColor = 1, 0.921568632, 0.0156862754, 1
	// Seconds.
	newVesselHighlightTimeout = 0.5
}
InfoOverlay
{
	// Vessel info hint overlay font size.
	fontSize = 10
	// RGBA format.
	backgroundColor = 0,0,0,0.5
	// RGBA format.
	textColor = 1,1,1,1
	// Pixels between the border and the text in the vessel info hint overlay.
	hintPadding = 3
}
CameraStabilization
{
	// Allowed values: None, KeepPosition, KeepDistanceAndRotation
	mode = KeepPosition
	// Seconds.
	animationDuration = 0.3
	// Meters.
	maxVesselDistance = 100
	// Alowed values: any KeyCode value.
	switchModeKey = F7
}
